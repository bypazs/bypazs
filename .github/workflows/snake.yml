# ════════════════════════════════════════════════════════════
#  Snake contribution animation generator
#  Place this file at:  .github/workflows/snake.yml
#  (create the folders if they don't exist)
#
#  After committing:
#   1. Go to your repo → Actions tab → enable workflows if prompted
#   2. Open "Generate Snake" → Run workflow (first run is manual)
#   3. It commits snake.svg to the `output` branch, which the README embeds
#  After that it auto-runs every 12 hours.
# ════════════════════════════════════════════════════════════
name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"   # every 12 hours
  workflow_dispatch:          # allow manual run
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Generate snake.svg (dark + green theme)
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/snake.svg?palette=github-dark&color_snake=00FF41&color_dots=0D1117,003B00,00FF41,00FF41,00FF41

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
